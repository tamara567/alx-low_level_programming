#include "main.h"

/**
* _isupper - checks for uppercase character
* @c: Variable text
* Return: Always 0 or 1
*/

int _isupper(int c)
{
	if (c >= 'A' && C <= 'Z')
		return (0);
	else
		return (0);
}
